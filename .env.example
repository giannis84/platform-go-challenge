# Application ports
API_PORT=8000
HEALTH_PORT=8001

# PostgreSQL
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=favourites
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
# Host port for accessing Postgres from outside Docker (e.g. psql from host)
POSTGRES_HOST_PORT=5432

# JWT signing secret (optional).
# When empty, only unsigned tokens (alg=none) are accepted — easier for local dev.
# In production, a secret is supposed to be fetched securely from a secrets server.
# JWT_SECRET=
